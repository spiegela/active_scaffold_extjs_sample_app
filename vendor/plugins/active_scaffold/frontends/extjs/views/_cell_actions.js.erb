new Ext.ux.grid.CellActions({
  listeners:{
    action:function(grid, record, action, value, index) {
//      Ext.ux.Toast.msg('Event: action', 'You have clicked: <b>{0}</b>, action: <b>{1}</b>/<b>{2}</b>', value, index, record.id);
	  Ext.Ajax.request( {
	  url: '/' + index + '/' + action + '/' + record.id,
      params: {
	    <%= request_forgery_protection_token %>: "<%= form_authenticity_token %>"
	    }
      });
  }
 }, align:'left'
});